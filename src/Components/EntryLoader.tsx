import React from 'react';

const EntryLoader = () => (
    <div style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        minHeight: '100vh',
    }}>
        <p>Loading...</p>
    </div>
);

export { EntryLoader };
export default EntryLoader;